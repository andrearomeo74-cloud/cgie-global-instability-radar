# CGIE 2.0 — CF-RETRO-01
# Frozen Python dependencies
#
# Recommended Python version:
# Python 3.11
#
# Installation command:
#
# python -m pip install --upgrade pip
# python -m pip install -r requirements-cgie2.txt
#
# These versions must not be changed during CF-RETRO-01 without
# creating a documented protocol amendment.

pandas==3.0.5
requests==2.34.2
PyYAML==6.0.3


DOVE INSERIRLO:

1. Apri il repository:

   cgie-global-instability-radar

2. Resta nella pagina principale del repository.

3. Non entrare nelle cartelle docs o cgie2.

4. Premi:

   Add file

5. Premi:

   Create new file

6. Nel campo del nome del file scrivi esattamente:

   requirements-cgie2.txt

7. Incolla soltanto il contenuto compreso tra:

   # CGIE 2.0 — CF-RETRO-01

   e:

   PyYAML==6.0.3

8. Nel campo Commit message inserisci:

   Freeze CF-RETRO-01 Python dependencies

9. Nel campo Extended description inserisci:

   Adds the pinned Python dependencies required by the CF-RETRO-01
   catalogue acquisition pipeline. The versions are frozen to improve
   reproducibility across local and GitHub Actions executions.

10. Seleziona:

    Commit directly to the main branch

11. Premi:

    Commit changes

NON ESEGUIRE ANCORA LO SCRIPT.

Il file deve trovarsi esattamente qui:

cgie-global-instability-radar/
└── requirements-cgie2.txt

Il prossimo commit creerà:

.github/workflows/cf_retro_01_download.yml

Questo workflow permetterà di eseguire il download del catalogo
direttamente da GitHub Actions, senza usare Python sul telefono o sul
computer.
